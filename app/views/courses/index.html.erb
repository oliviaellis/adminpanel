<%= render "shared/course_header" %>
<%= render "shared/navbar" %>
<div class="list_container" id="course_list">
  <% @courses.each do |c| %>
    <div class="list_item">
      <h2><%= link_to c.name, admin_course_path(id: c.id) %></h2>
      <!-- <h4><%= link_to "Delete course", admin_course_path(id: c.id), remote: true, method: :delete, data: { confirm: 'You sure?'} %></h4> -->
    </div>
  <% end %>
  <br>
  <div class="new">
    <%= link_to 'New', new_admin_course_path %>
  </div>
</div>
